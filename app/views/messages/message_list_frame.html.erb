<%= turbo_frame_tag "message_list" do %>
  <%= render MessageListComponent.new(messages: @messages, folder: @folder, current_contact: current_contact, active_filters: @active_filters || {}, labels: @labels || []) %>
<% end %>
